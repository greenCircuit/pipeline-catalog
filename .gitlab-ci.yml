stages:
- build

include:
  - pipelines/template.yml

no_var:
  extends: .build-job
  
one_var:
  extends: .build-job
  variables:
    TEST_ENV: "production"

two_var:
  extends: .build-job
  variables:
    TEST_ENV: "production"
    RUN_SCRIPT: "true"
