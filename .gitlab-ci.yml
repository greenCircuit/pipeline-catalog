stages:
- build

include:
  - pipelines/template.yml

no_var:
  extends: .build-job
  
one_var:
  extends: .build-job
  variables:
    TEST_ENV: "I will print only once"

two_var:
  extends: .build-job
  variables:
    TEST_ENV: "I will print and go inside if statement"
    RUN_SCRIPT: "true"
