stages:
- build

include:
  - pipelines/defaults/stages.yaml
  - pipelines/template.yml

# running all pipelines in order to validate them
two_var:
  extends: .build-job
  variables:
    TEST_ENV: "I will print and go inside if statement"
    RUN_SCRIPT: "true"
