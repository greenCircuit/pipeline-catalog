apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: my-app
resources:
  - deployment.yaml
  - service.yaml
namePrefix: prod-
commonLabels:
  app: my-app
  env: production
images:
  - name: nginx
    newTag: "1.25.0"
