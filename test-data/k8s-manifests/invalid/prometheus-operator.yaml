apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: bad-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: my-app
  endpoints:
    - port: 9090
      interval: "not-a-duration-format"
      scheme: invalid-scheme
