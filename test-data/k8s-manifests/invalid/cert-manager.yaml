apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bad-cert
  namespace: default
spec:
  secretName: bad-cert-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames: "should-be-a-list-not-a-string"
